[
  {
    "name": "distance",
    "constraint": "distance(hleft,hright)<DIST_MAX && distance(hleft,lright)<DIST_MAX && distance(hleft,lleft)<DIST_MAX && distance(hright, lleft)<DIST_MAX && distance(hright, lright)<DIST_MAX && distance(lleft, lright)<DIST_MAX"
  },
  {
    "name": "HandsOnTop",
    "constraint": "hleft.y > lright.y && hleft.y > lleft.y && hright.y > lright.y && hright.y > lleft.y "
  },
  {
    "name": "LeftRightOrientation",
    "constraint": "hleft.x < hright.x && lleft.x < lright.x"
  }
]